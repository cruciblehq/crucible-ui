import type { Event } from './Event';

/**
 * Represents a focus event in Crucible UI.
 *
 * A focus event is triggered when an element gains focus, such as when a user
 * tabs to a form field or clicks/taps on an input.
 */
export interface FocusEvent extends Event { }
