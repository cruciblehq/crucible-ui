import type { Event } from './Event';

/**
 * Represents a press event in Crucible UI.
 *
 * A press event is triggered when the user activates a pressable element,
 * such as a button or touchable component, through an input action like a
 * click, tap, or keypress.
 */
export interface PressEvent extends Event { }
