/**
 * Context is a way to pass data through the component tree without having to
 * pass props down manually at every level. In this case, we define an empty
 * context type since Crucible UI does not yet require context data. For now
 * this works a type placeholder.
 */
export interface Context { };
